% Script to generate the inital Results.mat file

str_filename = 'Results.mat';

if exist(str_filename, 'file')
    delete(str_filename);
else
    Records.peak    = 0;
    Records.r       = 0;
    Records.c       = 0;
    Records.user    = 'test user';
    Records.img     = struct_record.img;
end

